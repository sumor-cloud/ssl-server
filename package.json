{
  "name": "@sumor/ssl-server",
  "description": "An SSL web server for Expressjs support HTTP/2",
  "version": "0.0.0",
  "license": "MIT",
  "repository": "sumor-cloud/ssl-server",
  "type": "module",
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-standard": "^17.1.0"
  },
  "files": [
    "lib",
    "LICENSE",
    "index.js"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "lint": "eslint .",
    "lint-fix": "eslint --fix .",
    "test": "node -v && echo \"Error: no test specified\" && exit 1"
  }
}
